'use client';

import type { HomeDashboardDTO } from '../lib/homeDashboardDTO';

interface Props {
  dto: HomeDashboardDTO;
}

// Placeholder — Phase 4 will replace this with the real dashboard UI.
export function HomeDashboardView({ dto: _dto }: Props) {
  return null;
}
