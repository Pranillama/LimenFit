# lib/auth

This folder is a temporary home for the session placeholder helper introduced in T1. It is owned by T4, which may either populate this folder with real session helpers or remove it entirely in favour of `lib/supabase/server`. See `session.ts` for the inline TODO that guides the T4 implementer.
