# Hooks

Shared cross-feature hooks that are not tied to a single feature.

Feature-owned hooks (workout session, plan picker, exercise picker, home feed) live under their respective `features/<feature>/hooks/` directories and will be authored in T5+ tickets.
