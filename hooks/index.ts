// Barrel for shared cross-feature hooks.
// Feature-scoped hooks (workout session, plan picker, exercise picker, home) live under features/<feature>/hooks/ and are authored in T5+ tickets.
export {};
